{% extends 'back/backbase.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    {# <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Cin</th>
                <td>{{ user.cin }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ user.gender }}</td>
            </tr>
            <tr>
                <th>Datebirth</th>
                <td>{{ user.datebirth ? user.datebirth|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ user.phone }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
       <a href="{{ path('app_user_admin_index') }}">back to list</a>

    <a href="{{ path('app_user_admin_edit', {'cin': user.cin}) }}">edit</a>
    
{{ include('back/user_admin/_delete_form.html.twig') }} #}


<br>
<div class="container">
    <div class="main-body">
    
          <div class="row gutters-sm">
            <div class="col-md-4 mb-3">
              <div class="card">
                <div class="card-body">
                  <div class="d-flex flex-column align-items-center text-center">
                  <img src="{{ asset('uploads/user/' ~ user.profilePicture) }}" alt="Admin" class="rounded-circle" width="150">
                    <div class="mt-3">
                      <h4 class="text-success mb-1">{{ user.lastname}} {{ user.firstname}}</h4>
                      <p class="text-warning mb-0">{% for role in user.roles %}
                {% if role == "ROLE_USER" %}Membre{% elseif role == "ROLE_ADMIN" %}Administrateur{% endif %}{% endfor %}</p>
                      <p class="text-muted font-size-sm">{{ user.createdAt | date('Y-m-d H:i:s') }}</p>
                    </div>
                  </div>
                </div>
              </div>
              
            </div>
            <div class="col-md-8">
              <div class="card mb-3">
                <div class="card-body">
                  <div class="row">
                    <div class="col-sm-3">
                      <h6 class="mb-0">CIN</h6>
                    </div>
                    <div class="col-sm-9 text-secondary">
                      {{ user.cin }}
                    </div>
                  </div>
                  <hr>
                  <div class="row">
                    <div class="col-sm-3">
                      <h6 class="mb-0">Email</h6>
                    </div>
                    <div class="col-sm-9 text-secondary">
                      {{ user.email}}
                    </div>
                  </div>
                  <hr>
                  <div class="row">
                    <div class="col-sm-3">
                      <h6 class="mb-0">Gender</h6>
                    </div>
                    <div class="col-sm-9 text-secondary">
                      {{ user.gender}}
                    </div>
                  </div>
                  <hr>
                  <div class="row">
                    <div class="col-sm-3">
                      <h6 class="mb-0">Date of Birth</h6>
                    </div>
                    <div class="col-sm-9 text-secondary">
                      {{ user.datebirth|date('Y-m-d')}}
                    </div>
                  </div>
                  <hr>
                  <div class="row">
                    <div class="col-sm-3">
                      <h6 class="mb-0">Phone</h6>
                    </div>
                    <div class="col-sm-9 text-secondary">
                      {{ user.phone}}
                    </div>
                  </div>
                  <hr>
                  <div class="row">
                    <div class="col-sm-12">
                      {{ include('back/user_admin/_delete_form.html.twig') }}
                    </div>
                  </div>
                </div>
              </div></div></div></div></div>
      
    {# <a class="btn btn-warning py-3 w-100 mb-2" target="__blank" href="{{ path('app_user_admin_edit', {'cin': user.cin}) }}">Edit</a>

{{ include('back/user_admin/_delete_form.html.twig') }}
<a class="btn btn-success py-3 w-100 mb-2" target="__blank" href="{{ path('app_user_admin_index') }}">Back to list</a> #}
    

{% endblock %}
