{% extends 'base.html.twig' %}

{% block title %}Categorypro{% endblock %}

{% block body %}
    <h1>Categorypro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorypro.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorypro.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorypro_index') }}">back to list</a>

    <a href="{{ path('app_categorypro_edit', {'id': categorypro.id}) }}">edit</a>

    {{ include('categorypro/_delete_form.html.twig') }}
{% endblock %}
